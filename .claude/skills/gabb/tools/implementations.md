# gabb_implementations

Find concrete implementations of an interface/trait/abstract class.

```
gabb_implementations file="src/interfaces.py" line=10 character=5
```

Point to an interface/trait definition to find all implementing classes.

**Parameters:**
- `file`, `line`, `character` - Position of interface/trait
- `limit` - Max results (default: 50)
