# Copy this file to .env and fill in your values
# The .env file is gitignored to keep your secrets safe

ANTHROPIC_API_KEY=your-api-key-here

# Docker socket path (uncomment if Docker SDK can't connect)
# On macOS with Docker Desktop, you may need:
# DOCKER_HOST=unix:///Users/YOUR_USERNAME/.docker/run/docker.sock
